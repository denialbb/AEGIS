# ---------------------------------------------------------
# Glide-Slope Guidance Parameters
# ---------------------------------------------------------
# Descent-rate limits per phase, phase-specific horizontal PD gains,
# target-blend smoothing, and early-translation during braking.

# Max descent rates per phase (m/s)
GLIDESLOPE_K_ALT = 0.8  # deprecated
GLIDESLOPE_RATE_POWERED_DESCENT = 100.0
GLIDESLOPE_RATE_HOVER = 10.0
GLIDESLOPE_RATE_TERMINAL = 5.0

# ---------------------------------------------------------
# Phase-Specific Horizontal Translation Gains (PD)
# ---------------------------------------------------------
PD_KP_POS_LATERAL = 0.2
PD_KD_VEL_LATERAL = 0.5

HOVER_KP_POS_LATERAL = 0.5
HOVER_KD_VEL_LATERAL = 3.0

TERMINAL_KP_POS_LATERAL = 0.8
TERMINAL_KD_VEL_LATERAL = 4.0

# ---------------------------------------------------------
# Horizontal Target Smoothing
# ---------------------------------------------------------
TARGET_BLEND_TICKS = 200  # ~10 s at 20 Hz

# ---------------------------------------------------------
# Early Translation During Braking
# ---------------------------------------------------------
PAD_OFFSET_EARLY_THRESHOLD = 500.0
PAD_OFFSET_EARLY_ALPHA = 0.03
